# Bostrom Journal [~](particles/QmVUdZRMuhuYUsyQfjzzwgjymdXsXzcgNeJ7bb6Eaeeave.md)

## Выпуск 2: Кодирование и интерпретация инструкций по обработке киберграфа [~](particles/QmTGJjRCYB3ahyUxTX5d9iajeGd3gkA3sWScnfhgXWAtd6.md)

### Метаданные выпуска [~](particles/QmQtzUoPVmet9f6yBuKAzoQDBd4BrWU26EFDupXDVgaKHf.md)

1783825200, 2026-07-12

Editor-in-chief: maxim-uvarov aka cyber-prophet `bostrom1h29u0h2y98rkhdrwsx0ejk5eq8wvslygexr7p8`

[github](https://github.com/cyber-prophet/bostrom-journal)

Journal address: `bostrom1q7zd65fsx8hvh788lyla8wxemlesf0djrhe0wa`.

### Оглавление [~](particles/QmRN2kzXMb4rxQriAJSj9LYLR18fJF54iSjEoQKzytevi5.md)

- [Киберграф ~](#киберграф-)
- [Благодарность ~](#благодарность-)
- [Основы ~](#основы-)
- [Новизна ~](#новизна-)
- [Соглашения ~](#соглашения-)
  - [Повторение ребра и подтверждение киберлинка ~](#повторение-ребра-и-подтверждение-киберлинка-)
  - [Отрицание ребра и киберлинка ~](#отрицание-ребра-и-киберлинка-)
  - [Обновление ранее опубликованного ребра ~](#обновление-ранее-опубликованного-ребра-)
  - [Цитаты ~](#цитаты-)
  - [Согласие и несогласие с содержимым ~](#согласие-и-несогласие-с-содержимым-)
  - [Довод за и против ~](#довод-за-и-против-)
  - [Вопрос и ответ ~](#вопрос-и-ответ-)
  - [Авторство: перенос из других медиумов ~](#авторство-перенос-из-других-медиумов-)
  - [Тождество: другое имя той же сущности ~](#тождество-другое-имя-той-же-сущности-)
- [Совместная работа над информацией ~](#совместная-работа-над-информацией-)
  - [Измеримый консенсус ~](#измеримый-консенсус-)
  - [Смена масштаба: тезис, файл, корпус ~](#смена-масштаба-тезис-файл-корпус-)
  - [Агенты над графом: вывод как переиспользуемое значение ~](#агенты-над-графом-вывод-как-переиспользуемое-значение-)
  - [Качество информации ~](#качество-информации-)
- [Разобранный пример: что на самом деле сказал Будда? ~](#разобранный-пример-что-на-самом-деле-сказал-будда-)
- [Применения ~](#применения-)
- [Чтение инструкций ~](#чтение-инструкций-)
- [Приложение: формат киберграфа ~](#приложение-формат-киберграфа-)

### Киберграф [~](particles/QmbzbkZdH1ABzBBSxJFMGEEteBEUpWuV2trAGwQXsckecQ.md)

Киберграф — это общее информационное пространство. Он связывает файлы — в этом выпуске это обычный текст или, для структур данных, JSON, — и у каждого файла есть свой адрес — CID (content identifier): IPFS-хеш, уникально идентифицирующий содержимое (безотносительно имени файла). Связи между ними — киберлинки.

Киберлинк — это утверждение подписанта: «эти два файла связаны». Подписант — агент (например, человек, ai, контракт или сенсор), который создал и подписал киберлинк. Киберграф хранит и связь, и того, кто её утверждает, — а смысл утверждения порождается при чтении, интерпретацией (см. «Соглашения»).

Технически киберграф — это структура данных из таких киберлинков, и каждый киберлинк — это подписанная запись (record) вида:

```
{
    cid_from: Qm1
    cid_to: Qm2
    neuron: Qm3
}
```

cid_from и cid_to — это CID двух связываемых файлов. Пара cid_from → cid_to — это *ребро* киберграфа, направленное из cid_from в cid_to; файл становится вершиной, когда его CID стоит в ребре. Поле neuron несёт CID подписанта (Qm3), поэтому сослаться на него можно, как на любой файл, и вершиной подписант становится по тому же правилу — когда его CID стоит в ребре. Точное кодирование зафиксировано в приложении «формат киберграфа».

Благодаря тому, что все три поля — cid_from, cid_to и neuron — простые текстовые строки, мы можем, например, объединять их в одну строку через запятую и от полученной строки считать IPFS-хеш — производный CID. Хеш двойки "Qm1,Qm2" — *адрес ребра*: он не содержит подписанта и потому один для всех, кто связал те же файлы, — он адресует то, *что* утверждается. Хеш тройки "Qm1,Qm2,Qm3" — производный CID киберлинка: он адресует акт конкретного подписанта — *кто* утверждает. Оба адреса можно использовать для формирования других киберлинков — это позволяет выпускать инструкции по интерпретации рёбер и конкретных киберлинков при помощи других киберлинков. До такого использования производный CID — только адрес: вершиной он, как любой CID, становится, когда стоит в ребре.

В этом выпуске журнала мы придумываем и осмысляем возможные подходы — открытые соглашения для кодирования и интерпретации инструкций по обработке киберграфа. Придуманное - лишь плод мысленных упраженений, который так и не уперся в практику и реализацию. Имплементация в жизни позволит быстро качественно улушчить структуру соглашений. Однако, это задача будущего.

### Благодарность [~](particles/QmYEk8HGQuHCAHr4QLHHa5BDApaAJm9hY5iBrYB7BmaASP.md)

Насколько мне известно, первым, кто задокументировал подобные соглашения, был @snedashkovsky в [semantic conventions](https://github.com/Snedashkovsky/cyber-semantic-conventions) — в июле 2021 года, за несколько месяцев до запуска Bostrom; примеры работали на тестовой сети. Сама форма, вероятно, родилась раньше, как реализация задумок на cyb.ai; чьи это идеи изначально, из сегодняшней точки не ясно — Сергей дал им имя и описание. Его документ кодирует объекты приложений — tweet, follow, avatar: CID специальной строки, например tweet, ставится в источник ребра и помечает цель как объект этого типа; кодирование этих CID совпадает с приложением «формат киберграфа». Там же предложен мета-уровень: соглашение само объявляется в графе киберлинками от корня semantic convention, и перечень соглашений читается из самого графа.

И, разумеется, вся модель киберграфа со многими её проявлениями, часть из которых описана в данном выпуске, — в гораздо большем объёме родилась и живёт в умах основателей @mastercyb и @cyborgshead.

### Основы [~](particles/QmUCu1o1kNx8iLRmkNtegEvWuyLFVoNav9qJ1HsuhBCqat.md)

Определение из главы «Киберграф» не упоминает блокчейн: тройка и производные CID заданы поверх IPFS-хеширования — это *формат* киберграфа, пригодный для любой среды, где записи публикуются подписанными. Впервые киберграф был реализован на блокчейне Bostrom.

Там у тех же вещей другие имена, и читатель встретит их в Bostrom, на cyb.ai и в первом выпуске журнала: подписант там — *нейрон* (neuron), а файл, чей CID стоит в ребре, — *частица* (particle). Из этого же словаря имя поля `neuron` в записи киберлинка.

Киберлинк Bostrom несёт ту же тройку, что и формат: cid_from и cid_to лежат в записи, а подписанта определяет подпись транзакции. Расходится лишь кодирование подписанта: в Bostrom это адрес bostrom1… (специфичный для блокчейна хеш того же ключа), в формате — CID публичного ключа (см. приложение «формат киберграфа»).

Сверх тройки блокчейн добавляет только метаданные публикации. Поскольку Bostrom — блокчейн на основе Cosmos SDK, каждый киберлинк содержится в транзакции, поэтому из сырых данных блокчейна можно прочитать ещё два факта:

- height и timestamp — когда киберлинк опубликован
- account sequence — собственный счётчик транзакций подписанта, идущий без пропусков

Каждая транзакция с киберлинками подписана ключом своего подписанта, а блок, который её несёт, подписан валидаторами. Любой может проверить эти подписи и, не доверяя тому, кто предоставил данные, убедиться, что конкретный подписант опубликовал конкретный киберлинк в известный момент времени. Доказательство распространяется и на содержимое: CID детерминирован от содержимого, поэтому включённый в блок киберлинк фиксирует, что содержимое обоих файлов существовало уже на момент публикации, — любой, у кого есть файл, вычисляет его CID и сверяет с записанным в киберлинке. Поскольку account sequence идёт без пропусков, можно прочитать каждую транзакцию подписанта, так что ни один его киберлинк не остаётся скрытым.

### Новизна [~](particles/QmQeRQrjwDGunpUumwTsDQpbvt5KVsRVueRtRp1ej1K1Sf.md)

Киберграф не вводит новых криптографических механизмов. Он собирает известные — контентную адресацию, подписанные журналы, реификацию, подписанные утверждения — и добавляет один ход: адресуемой по содержимому становится сама *связь* — в киберграфе это ребро, — а значит, общим операндом: адрес ребра не согласуют, а вычисляют по одному формату (см. приложение). Ниже — где уже используются похожие механики (а в Nostr — почти все сразу) и в чём отличие.

**Контентная адресация: IPFS/IPLD и Git.** «Имя есть хеш содержимого» — основа всей конструкции (CID), и она не нова. Git адресует коммиты, деревья и блобы хешем их содержимого, образуя Меркле-DAG; IPFS/IPLD обобщает это до адресуемого графа произвольных данных, на котором киберграф и стоит технически. Новое здесь не в адресации объектов, а в адресации связей между ними (см. ниже).

**Подписанные журналы без доверия: Secure Scuttlebutt и Certificate Transparency.** Довод «Основ» — account sequence без пропусков делает запись подписанта доказуемо полной — это свойство append-only журнала. Secure Scuttlebutt [Tarr et al., 2019] даёт каждому участнику подписанный журнал с порядковыми номерами: пропуск виден, реплику нельзя незаметно проредить — ровно как здесь. Certificate Transparency [RFC 6962] строит публичный Меркле-лог сертификатов, который любой аудитор проверяет, не доверяя издателю лога; ложная выдача сертификата обнаруживается именно потому, что лог нельзя переписать задним числом. Киберграф наследует это свойство — проверяемая полнота без доверия к поставщику данных, — но переносит его носитель с отдельного журнала каждого участника (SSB) и централизованного оператора (CT) на консенсус блокчейна и порядок транзакций.

**Подписанные события и социальный граф: Nostr.** Ближайшая живая система. Событие Nostr — `{id, pubkey, tags, content, sig}`, где `id` есть sha256 сериализованного события, а теги ссылаются на другие события и ключи; реакции и репосты надстраивают над этим социальный граф подписанных ссылок. Совпадает почти всё: подпись ключом, адрес от содержимого, рёбра между сообщениями. Отличие — в статусе ребра. В Nostr тег — это ссылка *внутри* события, метаданные одного автора; у связи нет собственного детерминированного адреса, общего для всех, — чтобы целить в чужую связь подтверждением, отрицанием или доводом, отдельному соглашению пришлось бы сначала определить, что считать связью, и дать ей вычислимое имя. В киберграфе запись не несёт ничего, кроме связи и подписанта, а `hash(A, B)` — самостоятельный адрес: связь адресуема до и помимо того, кто её опубликовал, и потому рекурсия соглашений (подтвердить ребро, оспорить подтверждение, привести довод против довода) однородна. Реле Nostr к тому же не дают общего порядка; здесь его даёт блокчейн.

**Реификация и тождество: RDF.** Приём «сделать высказывание объектом, чтобы высказываться о нём» — это реификация RDF: тройка (subject, predicate, object) представляется ресурсом `rdf:Statement`, о котором можно делать новые тройки; RDF-star [W3C, 2021] делает высказывание термом самого языка. `same` прямо повторяет `owl:sameAs`. Разница — в происхождении идентичности реифицированной связи. В классической реификации ресурс высказывания именуется выданным IRI: чтобы два автора говорили об одном высказывании, они должны согласовать имя. RDF-star снимает это структурно — цитируемая тройка тождественна по своим компонентам, — но компоненты остаются IRI, выданными именами: согласование спускается на их уровень, а с содержимым за IRI имя ничем не связано. В киберграфе компоненты — CID: адрес ребра *вычисляется* — `hash(A, B)`, — поэтому он один для всех и криптографически связан с содержимым A и B. Это и есть тот единственный новый ход: контентная адресация, применённая к ребру.

**Подписанные утверждения и доверие: PGP Web of Trust и Verifiable Credentials.** Авторство как киберлинк (`hash("author", B) → P`) и подпись под каждым киберлинком — это семейство «сделать неявное доверие явным и проверяемым». PGP Web of Trust [Zimmermann] заменяет центральный удостоверяющий центр сетью взаимных подписей ключ–личность; W3C Verifiable Credentials [W3C VC] стандартизует утверждение, в котором издатель подписывает заявление о субъекте, а проверяющий удостоверяет подпись. Перенос дискуссий из других медиумов (раздел «Авторство») — тот же шаг, что отделяет VC от голого From-заголовка: атрибуция из строки превращается в подписанное адресуемое утверждение. Отличие — в экономии примитивов: роли издатель/держатель/субъект и реестры отзыва VC, как и слои доверия WoT, здесь сворачиваются в киберлинки плюс `deny`, а вес подписанта остаётся политикой чтения, а не частью протокола.

**Связь с содержимым и видимое переиспользование: Xanadu.** Старейшая формулировка программы — Project Xanadu [Nelson, 2000], первый гипертекстовый проект, начатый в 1960 году. Его требования к электронной литературе читаются как перечень свойств киберграфа: ссылки, которые не ломаются при смене версий, и *transclusion* — то же содержимое, узнаваемо присутствующее более чем в одном месте, с видимым происхождением каждой выдержки. Ксаналогическая структура различает ровно два вида связи — между разным (content link) и между тем же самым (transclusion); связь не встраивается в документ, а накладывается снаружи и целит в содержимое, а не в место: HTML-ссылку Нельсон сравнивал с витриной — видно то, что сейчас выставлено по адресу. Отличие — в происхождении тождества. У Нельсона адрес содержимому выдаётся при регистрации: каждый опубликованный символ получает постоянный адрес, поэтому тождество прослеживается только через операции самой системы — независимо записанный тот же текст получает другой адрес, — а работать это могло лишь как отдельная всемирная инфраструктура. Она так и не была построена: за шесть десятилетий — прототипы, демо и открытый код (Udanax, 1999), но не работающая сеть. Киберграфу программа Xanadu достаётся бесплатно и технически элегантно: адрес вычисляется из содержимого, поэтому одинаковое содержимое — один файл, кем бы и когда бы оно ни было записано, ссылка на CID не может сломаться, transclusion встроен в адресацию, а происхождение выдержки делает видимым соглашение `quote` — свойство адреса, а не отдельная система.

### Соглашения [~](particles/QmcuxCyzLd1uQGSh32AgfMtqe3e1oRo52dZPqEh4sEntkL.md)

У опубликованного ребра `A → B` есть адрес — `hash(A, B)`, CID от A и B, взятых вместе через запятую; на него можно ссылаться, как на любой другой CID.

Это позволяет нам формировать производные CID: как только ребро адресуемо, мы можем ссылаться на него, хешировать его вместе с другими CID и строить структуры, которые ссылаются на рёбра — и на рёбра о рёбрах.

Мы используем это, в частности, чтобы поверх сырого графа строить инструкции для его обработки. Инструкция выпускается путём размещения специального CID — такого как `hash("deny")` или `hash("quote", A)` — в источнике обычного ребра. Поскольку каждое такое использование само является адресуемым ребром, каждую инструкцию, в свою очередь, можно подтвердить, отвергнуть или процитировать теми же соглашениями. Эта рекурсия делает набор соглашений открытым.

Построение инструкций из собственных элементов графа — это разновидность *гомоиконичности* (свойства, знакомого из языков программирования, где код и данные имеют одно представление). Здесь этим представлением является CID: инструкция состоит из тех же CID и киберлинков, что и граф, который она обрабатывает, поэтому программа, обрабатывающая граф, живёт внутри него. И это справедливо на любом масштабе: весь киберграф в любой момент можно зафиксировать как файл, адресуемый по CID, вычисленному над ним.

Контентный адрес выводится из самого содержимого: одинаковое содержимое всегда даёт один и тот же CID. Поэтому одинаковое содержимое, кем бы и когда бы оно ни было записано, — один файл, а не копии: адрес — сразу и ссылка, и тождество; тождественное не хранится дважды. Эти два свойства граф не разделяет — их даёт один акт записи.

Киберграф — это структура данных общего назначения: пригодная для широкого спектра задач и не привязанная к блокчейну (см. формат в «Основах»); реализация Bostrom при этом ещё и permissionless. Писать в него может любой обладатель пары ключей, и любой может читать и обрабатывать его по-своему: привратника, который решал бы, кому писать и что считать действительным, здесь нет. Блокчейн проверяет только техническое соответствие транзакции — подпись и порядок, — но не смысл и не действительность записанного; защиту даёт криптография, контроль над своими записями остаётся за каждым подписантом, а записанное неизменно. Поэтому сырой граф содержит только CID и киберлинки и не несёт встроенного смысла: смысл порождается при чтении — приложение следует соглашениям, которые выбирает само.

И соглашения не обязаны быть всеобщими. Потребители графа приходят из разных сфер и изобретают механики под свои задачи; чтобы механика заработала внутри коллектива, коллектив должен о ней договориться. На этом и стоит совместная работа над информацией: общая неизменная основа — и свободно выбираемые поверх неё консенсусные механики, в том числе динамические.

Разделы ниже дают базовые примеры таких соглашений — как *кодировать* и как *интерпретировать* инструкции для обработки киберграфа. (`hash()` — всегда одна и та же операция: вычислить CID строки из аргументов, объединённых через запятую; см. приложение.)

#### Повторение ребра и подтверждение киберлинка [~](particles/QmZv48CqP6BiG6unQGXfQTMme9fXqzhQewsrWwFad13NAt.md)

Просто повторите ребро.

В отличие от `deny`/`update`/`quote`, повторение не требует специального CID в источнике: это просто то же самое ребро, заново опубликованное другим подписантом, и именно оно учитывается агрегацией (см. «Измеримый консенсус»). Соглашения здесь и нет — строки, которая шла бы в `hash()`, у повторения не существует.

Повторить можно ребро, но не чужой киберлинк: в тройке стоит подписант, поэтому заново опубликованное ребро — всегда собственный акт и собственное утверждение. Поэтому у `deny` киберлинка есть зеркало:

```
A - CID
B - CID
neuron - CID подписанта подтверждаемого киберлинка

hash("confirm") → hash(A, B, neuron)   # киберлинк действителен
```

`confirm` удостоверяет чужой акт — запись действительна, это не спам, — не утверждая содержания; тот же разрыв между записью и утверждением, на котором стоят две цели `deny` (см. «Отрицание ребра и киберлинка»). Подлинность записи доказывает подпись (см. «Основы»), и удостоверять её незачем; `confirm` — суждение о действительности.

Так закрывается единственная пустая клетка: у файла есть `agree`/`disagree`, у ребра — повторение и `deny`, у киберлинка был только `deny`. Повторение требует собственной работы — архивист сверил копии источника, агент повторил вычисление, читатель проверил подстроку цитаты, — и только тогда он утверждает то же самое. `confirm` выражает то, чего повторением выразить нельзя: поддержку за чужой счёт. Монах, сам не слышавший слов от Будды, не может повторить ребро «так я слышал» — это была бы ложная заявка, — но может удостоверить акт Ананды. Агент, взявший чужой вывод не пересчитывая, тем же ребром отмечает, на чей акт он опёрся. Отсюда и противовес: киберлинк, отвергнутый как спам, другие подписанты удостоверяют как действительный, и обе стороны видны рядом. В уровень поддержки самого утверждения `confirm` не входит: его даёт повторение ребра.

#### Отрицание ребра и киберлинка [~](particles/QmeM8bLpiF2ATfTVcUUXv4sTFnGSPFuve9qVPUv9yTgKxg.md)

```
A - CID
B - CID
neuron - CID подписанта оспариваемого киберлинка

hash("deny") → hash(A, B)          # отрицание ребра
hash("deny") → hash(A, B, neuron)  # отрицание конкретного киберлинка
```

Две цели отрицания — две семантики:
- `hash(A, B)` — оспаривает само утверждение A → B: оно неверно, кто бы его ни опубликовал
- `hash(A, B, neuron)` — оспаривает конкретный киберлинк конкретного подписанта: запись недействительна — например, как спам, — без мнения о самом утверждении

В отличие от `update` и `quote`, которые встраивают свой операнд в CID источника, `hash("deny")` — это фиксированный CID; отрицается то, на что он указывает.

#### Обновление ранее опубликованного ребра [~](particles/QmPg814p31xeMGrbxK9kWRZiJErZ6R7KcpF7jw929qaQXZ.md)

```
A - CID из исходного ребра
B - другой CID из исходного ребра
C - новый CID, замещающий B

A → B                            # исходное ребро
A → C                            # вспомогательное ребро — C достижим из A (необязательное)
hash("update", hash(A, B)) → C   # авторитетное — помечает C как обновление ребра
```

Обновления заново исходят из того же CID `hash("update", hash(A, B))`, поэтому обновление само может быть обновлено: самое свежее ребро из этого источника — текущая версия. Источник один для всех, поэтому опубликовать обновление может кто угодно; чьи обновления принимать — например, только подписанта исходного киберлинка — решает политика чтения. И как любое ребро, ребро обновления само адресуемо — `hash("update", hash(A, B)) → C` может быть подтверждено или отвергнуто другим подписантом.

#### Цитаты [~](particles/QmTPEavrjDRbcVcL2YdUS7JkDvRWxDJ4pJyW4zc2yAtSMw.md)

```
A - CID
B - CID, content(B) — подстрока content(A) (независимо проверяемо)

hash("quote", A) → B       # "B — добросовестная выдержка из A"
```

Когда ребро-цитату повторяет другой подписант, его повторение утверждает, что выдержка добросовестна; `hash("deny") → hash(hash("quote", A), B)` оспаривает её как вводящую в заблуждение вне контекста.

#### Согласие и несогласие с содержимым [~](particles/QmekU5SQv9H9eLid3boGx3uaqgLbtr8UMTa9sNSABmDegj.md)

```
B - CID оцениваемого файла

hash("agree") → B       # "тезис B верен"
hash("disagree") → B    # "тезис B неверен"
```

Повторение и `deny` оценивают рёбра: повторение утверждает то же ребро, `deny` его оспаривает. К отдельному файлу они неприменимы: у голого CID B нет ребра, которое можно повторить или отвергнуть. Пара `agree`/`disagree` даёт мнению о содержимом собственные рёбра — и дальше они агрегируются как любые другие: киберлинки разных подписантов с одним ребром складываются в уровень поддержки (см. «Измеримый консенсус»).

Пара `agree`/`disagree` и `deny` — разные оси. Первая говорит о содержимом: B верно или неверно. А `deny` ребра говорит об утверждении: A → B неверно; `deny` киберлинка — о записи: киберлинк конкретного подписанта недействителен. Поэтому несогласие с содержимым не выражается через `deny` чужого согласия — иначе «я не согласен с B» и «это согласие недействительно» стали бы неразличимы. Оси сочетаются: `hash("deny") → hash(hash("agree"), B, neuron)` оспаривает согласие конкретного подписанта — например, как спам, — не выражая мнения о самом B.

#### Довод за и против [~](particles/QmZqQxcXJ8ymNvk1nsF5CCgKSwzGweZXJ1wRpCNpmAJReA.md)

```
C - любой CID: файл, ребро или киберлинк
B - CID файла-довода

hash("pro", C) → B      # "B — довод за C"
hash("con", C) → B      # "B — довод против C"
```

Пара `agree`/`disagree` записывает позицию, но в живом споре основная масса высказываний — не «верно/неверно», а *почему*: довод, свидетельство, ссылка на источник. Довод — это отношение «B поддерживает C», и оно не утверждает истинности ни B, ни C: подписант может считать довод относящимся к делу, не соглашаясь ни с ним, ни с утверждением. Операнд C — любой CID: довод приводят и к файлу, и к ребру или киберлинку — например, против чужой атрибуции.

Форма повторяет логику `update`: операнд встроен в источник, поэтому `hash("pro", C)` — «сторона "за" C» — один адрес для всех подписантов. Зная C, читатель вычисляет один хеш и читает исходящие рёбра — это весь запрос; уровень аргументированности — исходящая степень. Симметрично `hash("con", C)` — сторона «против». (Альтернатива `hash("pro", B) → C` направила бы рёбра в сам операнд, но запрос «все доводы за C» потребовал бы разбирать источник каждого входящего ребра.) И как любое ребро, довод сам адресуем: его повторяют, оспаривают `deny`'ем, а к файлу B применяют `agree`/`disagree` — спор о качестве довода ведётся теми же соглашениями.

#### Вопрос и ответ [~](particles/QmYZcp14VXTBCPub19BR3jPbkpbXWhzHbza3A4q1DW45eU.md)

```
B - любой CID: файл, ребро или киберлинк
Q - CID файла-вопроса
A - CID файла-ответа

hash("question", B) → Q                            # "Q — вопрос о B"
hash("answer", hash(hash("question", B), Q)) → A   # "A — ответ на вопрос Q о B"
```

Что Q — вопрос, видно из его содержимого; помечать это отдельным ребром незачем. Соглашение несёт то, чего в содержимом нет: адрес — о чём спрошено. Операнд B — любой CID: файл, ребро или киберлинк. `hash("question", B)` — один вычислимый адрес «вопросы о B»: зная B, читатель вычисляет один хеш и читает исходящие рёбра — открытые вопросы; повторение ребра другим подписантом — «у меня тот же вопрос». Пара `agree`/`disagree` к Q неприменима: содержимое вопроса ничего не утверждает. Вопросное ребро — как любое ребро — оспаривают `deny`'ем, например как спам.

Ответ привязывается не к голому Q, а к вопросу-в-контексте — адресу вопросного ребра (тот же приём, что `deny` цитаты). Тексты вопросов часто неавтономны («Is there a consensus on this?»), и один Q, заданный о разных B, склеил бы ответы в общую вершину. Адрес ребра не содержит подписанта, поэтому один ответ обслуживает всех, кто повторил вопрос. Сам ответ — обычный файл: его оценивают `agree`/`disagree`, к нему приводят доводы `pro`/`con`, а лучший ответ выявляет агрегация, а не назначение.

#### Авторство: перенос из других медиумов [~](particles/QmTYj2nH8evSePMsmZ2LgqQ18rMNkraromiYZGKky66JLq.md)

Подписант киберлинка — тот, кто записал ребро в граф, и он не обязан быть автором файла. Это различие открывает граф для переноса: дискуссии, прожитые в других медиумах — Usenet-треды, переписки, форумы — уже имеют авторов, даты и рёбра ответов, но их авторство держится на доверии к серверам и архивам (From-заголовок — строка, а не подпись). Перенося такую дискуссию, архивист публикует файл и утверждает авторство отдельным ребром:

```
P - CID идентификатора автора (например, hash("satoshin@gmx.com"))
B - CID перенесённого файла

hash("author", B) → P     # "автор файла B — P"
```

Предмет — в источнике, как у `pro`/`con` и `question`: читая B, читатель вычисляет один хеш и читает исходящие рёбра — атрибуция распознаётся, даже если автор раньше в графе не встречался. Конкурирующие атрибуции исходят из одного адреса `hash("author", B)` и видны рядом, как версии у `update`. (Зеркальная форма `hash("author", P) → B` отвечала бы одним хешем на «все работы P», но основной запрос — обратный, и атрибуцию от неизвестного автора она делает невычислимой вперёд.)

Подпись под киберлинком принадлежит архивисту и удостоверяет акт переноса, а не само авторство: доверие, которое в исходном медиуме было неявным, становится явным адресуемым утверждением. Дальше оно живёт по общим правилам: независимые архивисты, сверившие свои копии источника, повторяют ребро — атрибуция набирает измеримый консенсус; ошибочную атрибуцию оспаривают `deny`'ем. А перенесённый файл сразу доступен всем соглашениям выше: тезисы давнего треда можно разложить рёбрами-цитатами и подписывать `agree`/`disagree` сегодня — спор, начатый в другом медиуме, продолжается в графе без потери авторства. Тем же приёмом пользуется разобранный пример ниже: «Так я слышал» — Ананда подписывает происхождение слов, произнесённых не им.

#### Тождество: другое имя той же сущности [~](particles/QmbsWv6d9wTwAim4mC21QAMyq2v447KAymcwwVWR8tWP1x.md)

```
P1, P2 - CID, именующие одну сущность

hash("same", P1) → P2     # "P1 и P2 — имена одной сущности"
```

Одна сущность живёт в графе под многими именами: `hash("satoshin@gmx.com")`, `hash("satoshi@vistomail.com")` и `hash("Satoshi Nakamoto")` — разные CID, и атрибуции, собранные под разными именами, не складываются при агрегации. Ребро `same` утверждает тождество (аналог owl:sameAs из семантического веба): сущность читается как кластер — компонента связности по рёбрам `same`; какие рёбра принять в кластер, решает политика чтения.

Тождество симметрично по смыслу, но ребро направлено: зеркальное ребро — другая двойка, и агрегация считает её поддержку отдельно; читать ли связность без учёта направления и складывать ли оба направления в одно утверждение, решает политика чтения — как и состав кластера. Ложное тождество оспаривают `deny`'ем. Операнд — любой CID, в том числе CID подписанта: ребро `hash("same", hash("Satoshi Nakamoto")) → neuron`, опубликованное самим подписантом, — заявка «это я», и перенесённые авторы (см. выше) соединяются с живыми участниками графа, подтверждающими или оспаривающими заявку. Одна оговорка: `same` действует при чтении и не отменяет формата (см. приложение) — в производные CID имя вшито хешем, и задним числом они не пересчитываются.

### Совместная работа над информацией [~](particles/QmZFsNwVPfBaUkHbGjAkSNer2SHWUF8DjyBVrK5aZWGAR4.md)

Киберлинки связывают файлы, а через них — и подписантов:

```
Alice утверждает:  A → B
Bob подтверждает:  A → B
Carol отрицает:    hash("deny") → hash(A, B)
Dave цитирует:     hash("quote", A) → B
```

Граф кодирует не только «что с чем связано», но и «кто во что верит» — и это поддаётся запросам и анализу.

#### Измеримый консенсус [~](particles/QmPR5uEZrnQYBaVWK8ef2Ws6js1qWqduqwXogyJhkjCAHA.md)

Для любого утверждения `A → B`:
- Посчитать киберлинки разных подписантов с этим ребром → уровень поддержки
- Посчитать киберлинки-отрицания → уровень спора
- Посчитать `confirm` и `deny` конкретных киберлинков → действительность записей (отдельная ось: в уровень поддержки не входит)
- Взвесить подписантов по политике чтения (например, по репутации) → взвешенный консенсус
- Отслеживать во времени → эволюция консенсуса

Действительность не решает ни один центральный орган. Вместо этого:
- Делаются утверждения (создаются киберлинки)
- Другие подтверждают или отрицают (новые киберлинки)
- Агрегация выявляет консенсус
- Мнения меньшинства остаются видимыми (не цензурируются, просто оказываются в меньшинстве)

```
Consensus(statement) = f(repeats, denies, signer_weights, time_decay)
```

Точная формула — это вопрос политики чтения. Киберграф предоставляет сырые данные.

Мнение подписанта тоже живёт во времени: можно подтвердить утверждение, а позже отвергнуть его. Киберлинки подписанта упорядочены (см. «Основы»), поэтому его актуальное мнение естественно читать как последнее по времени; прежние остаются видимой историей. Тем же способом отзывается собственный киберлинк: `hash("deny") → hash(A, B, neuron)`, где neuron — CID самого отзывающего.

#### Смена масштаба: тезис, файл, корпус [~](particles/QmPzijfgdPnfKBA6QS1Ne9CiLeVsq745UrWAPn1nCXMqFi.md)

Текстовый файл A не обязательно рассматривать как единое целое. Его можно разложить на базовые тезисы, которые он делает: каждый тезис — дословная выдержка — публикуется как отдельный файл B и связывается с исходным ребром-цитатой `hash("quote", A) → B` (см. «Цитаты»). Разложение ограничено дословным: подстроку проверяет любой; нормализованный парафраз строится из выдержки отдельным шагом и в это соглашение не входит. Теперь у каждого тезиса есть собственный CID, и он по отдельности доступен для оценочных киберлинков — `agree` и `disagree` (см. «Согласие и несогласие с содержимым»).

Когда оценки от разных подписантов накапливаются, по каждому тезису складывается своя статистика, а файл A в целом получает их агрегат. И поскольку тезис адресуется по содержимому, его оценки не привязаны к файлу A: автор другого текста может включить уже оценённый тезис в свой файл, и оценочная статистика придёт вместе с ним автоматически — CID тезиса тот же.

По всему множеству файлов это открывает анализ за пределами подсчёта подтверждений: кластеризацию связанных файлов, выявление центральных тенденций и выбросов, достойных исследования, и обнаружение файлов, которые противоречат друг другу или утверждают одно и то же разными словами.

Вместе это даёт работе над информацией смену масштаба: можно приблизиться — от корпуса к файлу, от файла к отдельному тезису, — оценить деталь и отдалиться обратно, и оценки соберутся в свойства целого. Уровни не требуют разных механизмов: тезис, файл и корпус адресуются CID, и одни и те же соглашения действуют на каждом масштабе (см. «Соглашения»).

#### Агенты над графом: вывод как переиспользуемое значение [~](particles/QmaweEkw8r5ArxKWQLXFjySR565UVofsJYh6UKWZU4Gwzr.md)

Разнородным агентам, действующим сообща, нужны явные правила взаимодействия, которые каждый может проверить, не доверяя другому. Киберграф несёт такие правила как соглашения в самих данных — отдельный слой координации не нужен.

Подписант не обязан быть человеком. Контракт или LLM-агент подписывает киберлинки теми же соглашениями, и это меняет масштаб совместной работы: на один корпус можно натравить разные модели и зафиксировать выводы, которые сделала каждая.

Зафиксированный вывод — обычный файл с собственным CID, поэтому он сразу доступен всем соглашениям выше: его цитируют (`quote`), оценивают (`agree`/`disagree`), приводят к нему доводы (`pro`/`con`), оспаривают (`deny`). Но важнее, что он становится входом для следующего вывода: одна модель разложила текст на тезисы, другая связала их доводами, третья проверила на согласованность с корпусом — каждый шаг читает результаты предыдущих как готовые кирпичики, а не получает их заново.

Здесь работают оба свойства контентного адреса (см. «Соглашения»). Вывод LLM стоит дорого — токены, задержка, недетерминизм. Записанный один раз, он больше не пересчитывается: фиксация вывода ставит его CID в ребро от входа, и это ребро — запись в кэше: ключ — вход, значение — вывод. И кэш общий: ребро и файл видны всем — никому не нужно повторять чужую работу.

Отличие от обычного кэша вычислений в том, что значение оспоримо. Результат сборки в системе вроде Nix тоже адресуется по содержимому и переиспользуется, но оспорить его нельзя — он просто есть. Вывод в графе живёт по общим правилам: его подтверждают повторением, оспаривают `deny`'ем, измеряют консенсусом (см. «Измеримый консенсус»). Так кэш вычислений становится совместным знанием — единицей, которую агенты не только переиспользуют, но и проверяют друг за другом.

Фиксация вывода — обычная запись: вывод публикуется как файл и становится вершиной, когда его CID стоит в ребре. В содержимое вывода подписант может вписать происхождение — CID обработанных киберлинков и связанных ими файлов, а также описание произведённого вычисления. Происхождение — часть содержимого, поэтому CID вывода фиксирует его входы. Что делать с чужим выводом, решает политика чтения: доверять подписанту, измерить консенсус или повторить объявленное вычисление над теми же входами.

#### Качество информации [~](particles/QmRkxzDzgvCntLrZpRhiALvYdFsqDNZT37BLkEK9hkkQeQ.md)

Ссылка в письменной культуре не нова. В книге она есть, но за ней нет ни проверки, ни индекса: ни подтвердить, ни собрать все ссылки на один источник. Научный журнал добавил оба — рецензента, отвечающего за проверку, и индекс цитирования, — но дорого и медленно: рецензирование и публикация затратны, а выпуски привязаны ко времени. Киберграф усиливает каждую из этих возможностей и вдобавок демократизирует саму запись в общее пространство.

Переиспользование идёт на уровне отдельного тезиса, а не файла (см. «Смена масштаба»). Проверка становится коллективной, измеримой и непрерывной, с числом участников, недостижимым для ручного рецензирования (см. «Измеримый консенсус»). А корпус перестаёт быть запертым в своём медиуме: дискуссии из писем, форумов и устных традиций переносятся в граф и проверяются в нём заново (см. «Авторство»).

Вместе это открывает переработку накопленного корпуса. Скажем, можно количественно оценить, что общего у буддизма и адвайты — где они зовут одно и то же явление разными именами (`same`), а где расходятся, — и сделать это без бутылочного горлышка единственного эксперта: оценка складывается из многих подписантов. Раз зафиксированное, такое знание остаётся адресуемым, а потому доступным для дальнейшей обработки; когда появляются новые факты, корпус переобрабатывается с начала — теперь силами агентов (см. «Агенты над графом»). Концепты оказываются связаны с количественными метриками, и это поднимает discovery (обнаружение релевантной информации): «то же самое, сказанное иначе» граф не вычисляет как похожесть, а несёт как суждение — то же ребро `same`; эквивалентность кто-то утверждает, и потому её, как любое ребро, можно оспорить (`deny`) и измерить консенсусом.

### Разобранный пример: что на самом деле сказал Будда? [~](particles/QmQtYUJDrq4Jv8z74vZYLSCz9ujQa3ikvWLX5bC6dVYT6L.md)

Будда ничего не писал. Он учил около сорока пяти лет, умер, и в течение четырёх — четырёх с половиной столетий его слова передавались общинным чтением вслух — хранились в памяти, а не на письме. Не было оригинальной рукописи, по которой можно было бы проверить утверждение, и не было центрального органа, уполномоченного что-либо удостоверять. Методы, которыми традиция сохраняла и проверяла его слова, — собственные соглашения традиции, устроенные как определённые выше. (Дальше описана структура, которую фиксирует традиция; происходило ли каждое событие в точности так, как рассказано, — предмет споров; суть в форме.)

**Авторство — «Так я слышал» есть подписанный киберлинк.** Почти каждая беседа в каноне открывается одними и теми же словами: *Evaṃ me sutaṃ*, «Так я слышал». По традиции это слова Ананды — помощника Будды, пересказывающего то, что он лично слышал. Формула не претендует на авторство содержания; она удостоверяет происхождение: *я, эта личность, слышал это от того источника.*

```
neuron = Ananda
A = hash("heard from the Buddha")     # источник / акт слышания
B = hash(teaching)                    # беседа, адресуемая по содержимому

Ananda:  A → B                        # "Так я слышал"
```

Подпись несёт личность Ананды; `content(B)` — слова Будды в его пересказе. Открывающая формула почти каждой беседы — это ребро происхождения плюс личность слышавшего: ровно та тройка, которую несёт киберлинк. Специальный CID `hash("heard from the Buddha")` не входит в перечень выше — это собственное соглашение традиции: набор соглашений открыт (см. «Соглашения»).

Содержимое несёт больше, чем ребро. Каждая беседа открывается не только словами «Так я слышал», но и своей обстановкой. Один пример: *«однажды Благословенный пребывал близ Саваттхи, в роще Джеты»*. Обстановка называет место, аудиторию и повод. Вместе это — условия, при которых были услышаны слова; обстановка входит в content(B) и записана словами Ананды, не Будды. Положение Ананды тоже зафиксировано. Его помнят как первого среди тех, кто много слышал. Читатель, не знавший ничего из этого, всё равно мог бы вычитать эти измерения из самого рассказа и взвесить его соответственно.

Глубина простого «P сказал B» идёт не от одного ребра, а от того, кто его подписал, когда это было сделано и что корпус уже говорит об этом подписанте. Киберграф раскрывает то же самое для любого подписанта. Его декларация — это сам подписанный киберлинк. Контекст на момент действия — это его прежние киберлинки. Они идут без пропусков по account sequence, поэтому запись доказуемо полна: ни один киберлинк не прячется между двумя его транзакциями (см. «Основы»). А другие сделали о нём свои собственные киберлинки.

**Нет центрального органа — собственное соглашение Будды о проверке.** Предвидя свою смерть, Будда оставил процедуру для проверки любого учения, позднее приписанного ему, — *Mahāpadesa*, «Четыре великих ссылки» (DN 16; AN 4.180). Получив утверждение «я слышал это от Будды», следует

> «ни одобрять, ни отвергать» его на основании того, кто это говорит; вместо этого, «тщательно запомнив эти слова и фразы, вы должны убедиться, что они вписываются в беседы (сутты) и проявляются в дисциплине (винае). Если они не вписываются... вы должны их отбросить». — AN 4.180

Действительность решается согласованностью с существующим корпусом (Сутта и Виная — установленный граф), а не авторитетом заявляющего. Даже «я слышал это лицом к лицу от Будды» проверяется так же. Это действительность-через-согласованность — иная политика чтения, чем взвешенный по репутации подсчёт голосов выше, и устойчивая к нему: утверждение с множеством подтверждений всё равно не проходит, если оно противоречит корпусу. Нового механизма здесь нет — проверка читает существующий корпус, а сама процедура есть ещё одно такое же соглашение, чьё определяющее учение опубликовано в граф как обычный киберлинк.

*Kālāma Sutta* (AN 3.65) формулирует принцип прямо: не идите за устной традицией, за преемственностью, за слухами, за священным писанием, за кажущейся компетентностью говорящего или за «учитель — наш» — *знайте сами.* Здесь положение заявляющего полностью исключено из действительности — решают результаты, а не говорящий.

Канон, таким образом, есть граф киберлинков происхождения, управляемый соглашением о подтверждении и отрицании, которое сам Будда опубликовал в граф. Киберграф — то же устройство с криптографическими подписями и адресацией по содержимому, — так что «что на самом деле сказал Будда» становится доступным для запросов и перекрёстно проверяемым против корпуса, а не предметом веры.

Сохранность — отдельная от проверки забота. Цепочка хранит только CID; сами файлы живут вне её, и любая проверка — цитаты, согласованности с корпусом — читает содержимое. Канон дожил до записи не проверкой, а репликацией: четыре столетия общинная декламация держала текст в тысячах памятей. В киберграфе эту роль играет хранение содержимого (pinning): киберлинк неуничтожим, но осмыслен лишь до тех пор, пока кто-то хранит файлы, на которые он указывает.

И измерения для чтения подписанта открыты так же, как открыты соглашения выше: утверждения о его опыте, ссылки на внешние сервисы и импортированные из них метрики — каждое само есть киберлинк, который специализированные подписанты могут перепроверить, подтвердить или отвергнуть. Измерения растут через соглашения, а не через новый механизм.

### Применения [~](particles/QmR38m3uchY55BhVyEkDXcYWKZiddLDEAjG1zU17kbiSGp.md)

| Область | Применение |
|--------|-------------|
| **Наука** | Тезисы статьи как отдельные файлы; репликация = `agree`; неудавшаяся репликация = `disagree` |
| **Журналистика** | Проверка источников; несколько репортёров, подтверждающих один факт |
| **Право** | Условия договора; показания свидетелей; цепочки доказательств |
| **Стандарты** | Технические спецификации; согласие сообщества по соглашениям |
| **Образование** | Проверка знаний; рецензирование объяснений |
| **Выравнивание ИИ** | ИИ-агенты взаимодействуют через киберлинки; измеримая агрегация предпочтений |
| **Цепочки поставок** | Утверждения о продукте; сертификация; журналы аудита |

Двенадцати соглашений на одном примитиве — `confirm`, `deny`, `update`, `quote`, `agree`, `disagree`, `pro`, `con`, `question`, `answer`, `author`, `same` — плюс повторения ребра, у которого специального CID нет, достаточно, чтобы выразить сложные совместные рабочие процессы. Поскольку подписантами могут быть и люди, и контракты, те же соглашения применимы к взаимодействиям ИИ-агентов без дополнительной инфраструктуры.

### Чтение инструкций [~](particles/QmSv5znKB4iEBvvX81e3d2DguAkRpa7uXJJx7NtL2yDLdW.md)

CID непрозрачен: глядя на ребро `X → Y`, нельзя увидеть, что X — это, скажем, `hash("update", hash(A, B))`. Распознать инструкции читатель может двумя путями. Первый — вычислять кандидатов вперёд: считать производные CID от всего уже увиденного — рёбер и киберлинков — и сверять с ними источники и цели новых рёбер. Второй — разыменовать источник и прочитать его содержимое; он работает, только если прообраз производного CID — сама строка вида `"update,Qm…"` — опубликован как файл. Поэтому, выпуская инструкцию, публикуйте и прообразы её производных CID. Ребро, части которого не разбираются по этим правилам, — не инструкция, а обычное ребро: содержимое `"deny"` существует и вне соглашения.

Оба пути отражают текущий уровень развития киберграфа и приложений и пока остаются теорией. Важно, что киберграф уже даёт проверяемое доказательство публикации (см. «Основы»). Для помощи в индексации могут использоваться расширения — например, перечень всех сырых CID, вошедших в производные CID на концах ребра, записанный в memo транзакции (сейчас memo функционально не используется); перечень облегчает распознавание, а инструкцию по-прежнему несёт само ребро.

`hash("convention") → hash("deny")` — объявление соглашения: специальный CID соглашения публикуется в граф ребром из общего корня, а описание соглашения — обычный файл, связанный с тем же специальным CID ещё одним ребром. Зная корень, читатель вычисляет один хеш и читает исходящие рёбра — перечень объявленных соглашений; оба пути выше такого перечня не дают. Первым объявление соглашений в графе задокументировал @snedashkovsky — от корня semantic convention (см. «Благодарность»). Корень не закрывает набор и не назначает реестра: объявление — такое же соглашение, и кому нужен собственный перечень, тот выбирает свой корень и интерпретирует его по-своему. И как любое ребро, объявление подтверждают повторением и оспаривают `deny`'ем.

### Приложение: формат киберграфа [~](particles/QmbkvgkgKaWjpz5h16v2JRDWrNQ79FaiWjJinomh7uWrDW.md)

CID — всегда CID v0: содержимое оборачивается в стандартный IPFS-блок (UnixFS dag-pb), хешируется sha2-256, и хеш кодируется в base58 — получается строка вида Qm…. Тот же результат даёт `ipfs add --cid-version=0`.

Подписант (поле `neuron` в записи) — это CID его публичного ключа. В этом формат сознательно расходится с Bostrom, где подписант записывается адресом `bostrom1…` (см. «Основы»): формат берёт CID ключа, чтобы подписант был адресуем, как любой другой CID. Кодирование подписанта должно быть одно: если одного подписанта можно записать по-разному (адресом, голым ключом), один и тот же акт получит разные производные CID киберлинка. Ломается адресация акта: `deny` киберлинка, отзыв собственного киберлинка, оспаривание согласия конкретного подписанта — рёбра, посчитанные от разных кодировок подписанта, целят в разные CID, и их повторы не совпадают при агрегации. Страдает и взвешивание: один подписант под двумя кодировками читается как два. Адрес — хеш ключа, обратно ключ не вычисляется; для конверсии ключ достают из транзакции, несущей киберлинк, — транзакция публикует ключ подписанта рядом с подписью — и считают его CID — без этого шага производный CID киберлинка Bostrom не посчитать.

`hash(x, y, …)` — всегда CID строки из аргументов, объединённых через запятую; результат — производный CID. Два частных случая: адрес ребра — `hash(cid_from, cid_to)`, производный CID киберлинка — `hash(cid_from, cid_to, neuron)`. Конкатенация через запятую — частный выбор кодирования, взятый здесь ради наглядности: аргументы можно кодировать и иначе, например канонической сериализацией записи в JSON. Неизменно другое: кодирование должно быть одно для всех — иначе, как и с двумя кодировками подписанта выше, один и тот же акт получит разные производные CID.
